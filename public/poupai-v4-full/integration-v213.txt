(()=>{
'use strict';
const API=location.origin;
let selectedFile=null,busy=false,runSeq=0;
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
const money=v=>{const n=Number(v);return Number.isFinite(n)?n.toLocaleString('pt-BR',{style:'currency',currency:'BRL'}):'—'};
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const cleanCep=v=>{const d=String(v||'').replace(/\D/g,'').slice(0,8);return d.length===8?`${d.slice(0,5)}-${d.slice(5)}`:null};
const norm=s=>String(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim();
const style=document.createElement('style');style.textContent=`
.p213-analyzer{background:#fff;border:10px solid #071f42;border-radius:34px;overflow:hidden}.p213-head{padding:23px 28px;border-bottom:1px solid #e5ece7;display:flex;justify-content:space-between;gap:14px;align-items:center}.p213-head h3{margin:0;color:#071f42}.p213-live{padding:8px 11px;border-radius:999px;background:#eaf8ed;color:#16742b;font-size:10px;font-weight:950}.p213-body{padding:28px}.p213-grid{display:grid;grid-template-columns:1.25fr .75fr;gap:16px}.p213-upload,.p213-fields{border:1px solid #dfe8e2;border-radius:21px;padding:20px;background:#fbfdfb}.p213-upload{display:grid;place-items:center;text-align:center;min-height:230px}.p213-upload h4{margin:8px 0 5px;color:#071f42}.p213-upload p,.p213-help{color:#748596;font-size:11px}.p213-icon{font-size:30px}.p213-btn{border:0;border-radius:13px;padding:13px 16px;background:#071f42;color:#fff;font-weight:900;cursor:pointer}.p213-btn.primary{background:#20c63a;color:#06203b}.p213-btn[disabled]{opacity:.55;cursor:not-allowed}.p213-field{margin-bottom:13px}.p213-field label{display:block;font-size:10px;font-weight:950;color:#0b2749;margin-bottom:6px;text-transform:uppercase;letter-spacing:.04em}.p213-field input{width:100%;box-sizing:border-box;border:1px solid #d7e2da;border-radius:12px;padding:12px 13px;font-weight:800;color:#0b2749;background:#fff}.p213-file{margin-top:13px;padding:12px;border-radius:12px;background:#edf7ef;color:#176b2b;font-size:11px;font-weight:800;display:none}.p213-file.show{display:block}.p213-progress{display:none;margin-top:18px;border:1px solid #dfe8e2;border-radius:20px;padding:18px}.p213-progress.show{display:block}.p213-progress h4{margin:0 0 4px;color:#071f42}.p213-progress p{margin:0 0 12px;color:#718294;font-size:11px}.p213-track{height:8px;border-radius:999px;background:#e8efea;overflow:hidden}.p213-track i{display:block;height:100%;width:0;background:#20c63a;transition:width .25s}.p213-chips{display:flex;gap:7px;flex-wrap:wrap;margin-top:11px}.p213-chip{padding:6px 8px;border-radius:999px;background:#f0f3f1;color:#7b8b9a;font-size:9px;font-weight:900}.p213-chip.done{background:#eaf8ed;color:#17742b}.p213-results{display:none;margin-top:18px}.p213-results.show{display:block}.p213-extracted{border:1px solid #dfe8e2;border-radius:20px;padding:18px}.p213-extracted-head{display:flex;justify-content:space-between;gap:12px;align-items:center;margin-bottom:13px}.p213-extracted-head h4{margin:0;color:#071f42}.p213-verified{padding:7px 9px;border-radius:999px;background:#eaf8ed;color:#17742b;font-size:9px;font-weight:950}.p213-kpis,.p-kpis{display:grid;grid-template-columns:repeat(4,1fr);gap:10px}.p213-kpi,.p-kpi{background:#f6f9f7;border-radius:13px;padding:12px}.p213-kpi small,.p-kpi small{display:block;color:#7b8b9a;font-size:9px;text-transform:uppercase;font-weight:900}.p213-kpi b,.p-kpi b{display:block;color:#071f42;font-size:16px;margin-top:3px}.p-real{margin-top:16px;background:#fff;border:1px solid #dfe8e2;border-radius:20px;padding:18px}.p-real h4{margin:0 0 6px;color:#071f42}.p-real p{margin:0;color:#66798d;font-size:11px;line-height:1.45}.p-status{display:flex;justify-content:space-between;gap:12px;align-items:center}.p-badge{padding:8px 10px;border-radius:999px;font-size:10px;font-weight:950;background:#fff3df;color:#895b00}.p-badge.good{background:#e9f9ed;color:#13772a}.p-note{margin-top:11px;padding:10px 12px;border-radius:12px;background:#eef5ff;color:#285b91;font-size:10px;font-weight:800}.p-note.warn{background:#fff5df;color:#865d00}.p-block{background:#fffafa;border-color:#ead8d8}.p-block ul{margin:9px 0 0;padding-left:18px;color:#6e5961;font-size:11px}.p-offers{display:none!important}.p-offer{display:none!important}
@media(max-width:780px){.p213-analyzer{border-width:6px;border-radius:25px}.p213-head{padding:17px 16px;align-items:flex-start;flex-direction:column}.p213-body{padding:16px}.p213-grid{grid-template-columns:1fr}.p213-upload{min-height:190px}.p213-kpis,.p-kpis{grid-template-columns:1fr 1fr}.p-status{align-items:flex-start;flex-direction:column}}
@media(max-width:430px){.p213-kpis,.p-kpis{grid-template-columns:1fr}.p213-btn{width:100%}}
`;document.head.appendChild(style);
function mime(f){if(f.type)return f.type;const n=f.name.toLowerCase();if(n.endsWith('.pdf'))return'application/pdf';if(/\.jpe?g$/.test(n))return'image/jpeg';if(n.endsWith('.png'))return'image/png';if(n.endsWith('.webp'))return'image/webp';return''}
function b64(f){return new Promise((ok,no)=>{const r=new FileReader();r.onload=()=>ok(String(r.result).split(',')[1]||'');r.onerror=()=>no(new Error('Não foi possível ler o arquivo no aparelho.'));r.readAsDataURL(f)})}
async function post(path,body,timeout=65000){const c=new AbortController(),t=setTimeout(()=>c.abort(),timeout);try{const r=await fetch(API+path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body),signal:c.signal,cache:'no-store'});const j=await r.json().catch(()=>({}));if(!r.ok){const e=new Error(j.message||j.error||`Erro ${r.status}`);e.status=r.status;throw e}return j}finally{clearTimeout(t)}}
function setProgress(p,title,desc,chip){$('#p213Progress')?.classList.add('show');$('#p213Results')?.classList.remove('show');if($('#p213Bar'))$('#p213Bar').style.width=p+'%';if($('#p213Title'))$('#p213Title').textContent=title;if($('#p213Desc'))$('#p213Desc').textContent=desc;if(chip)$(chip)?.classList.add('done')}
function finish(){if($('#p213Bar'))$('#p213Bar').style.width='100%';setTimeout(()=>{$('#p213Progress')?.classList.remove('show');$('#p213Results')?.classList.add('show');$('#p213Results')?.scrollIntoView({behavior:'smooth',block:'start'})},180)}
function resetOutput(){$('#p213Results')?.classList.remove('show');if($('#p213Results'))$('#p213Results').innerHTML='';$$('.p213-chip').forEach(x=>x.classList.remove('done'));document.querySelector('#poupaiPaywall')?.remove()}
function canonicalProvider(v){const n=norm(v);if(n.includes('vivo')||n.includes('telefonica'))return'Vivo';if(n.includes('claro'))return'Claro';if(/(^| )tim( |$)/.test(n))return'TIM';if(n.includes('brisanet'))return'Brisanet';if(n.includes('unifique'))return'Unifique';if(n.includes('desktop'))return'Desktop';if(/(^| )nio( |$)/.test(n))return'Nio';return String(v||'').trim()||null}
function reconcileReader(reader,typedCep){const x={...(reader.extraction||{})},notes=[];x.provider=canonicalProvider(x.provider);if(typedCep)x.cep=typedCep;const ds=reader.verification?.disagreements||[];const pd=ds.find(d=>d.field==='internetMonthlyPrice');if(pd&&x.bundleDetected&&x.provider==='Vivo'){const a=Number(pd.before),b=Number(pd.after);if(Number.isFinite(a)&&Number.isFinite(b)){const hi=Math.max(a,b),lo=Math.min(a,b),gap=hi-lo;if(hi>=70&&hi<=220&&lo>=40&&gap>=8&&gap<=70){x.internetMonthlyPrice=hi;notes.push('Vivo Total: o Poupai reconciliou o subtotal comercial da Fibra com o componente fiscal/SCM.')}}}if(x.cep) x.cep=cleanCep(x.cep);return{x,notes,disagreements:ds}}
function extractCard(x,reader,notes){return `<div class="p213-extracted"><div class="p213-extracted-head"><h4>O que o Poupai confirmou na sua conta</h4><span class="p213-verified">${reader.verification?.passedWithoutCorrection?'✓ dupla leitura validada':'✓ leitura verificada'}</span></div><div class="p213-kpis"><div class="p213-kpi"><small>Operadora</small><b>${esc(x.provider||'Não identificado')}</b></div><div class="p213-kpi"><small>Plano</small><b>${x.speedMbps?esc(x.speedMbps+' Mbps'):'Não identificado'}</b></div><div class="p213-kpi"><small>Internet recorrente</small><b>${money(x.internetMonthlyPrice)}</b></div><div class="p213-kpi"><small>Fatura total</small><b>${money(x.invoiceTotal)}</b></div></div>${x.bundleDetected?'<div class="p-note warn">Combo identificado. A internet será comparada separadamente, mas a economia final precisa considerar possíveis efeitos nos outros serviços.</div>':''}${notes.length?`<div class="p-note">${notes.map(esc).join(' ')}</div>`:''}</div>`}
function block(x,reader,notes,items,title='Não foi possível concluir com segurança'){const r=$('#p213Results');r.innerHTML=extractCard(x||{},reader||{},notes||[])+`<div class="p-real p-block"><h4>${esc(title)}</h4><p>Em vez de preencher com dados incertos, o Poupai interrompeu esta análise.</p><ul>${(items||[]).map(i=>`<li>${esc(i.message||i.code||i)}</li>`).join('')}</ul></div>`;finish()}
function offerHidden(o,i){return `<article class="p-offer"><small>#${i+1}</small><h5>${esc(o.provider||'Operadora')} • ${esc(o.planName||'Plano')}</h5><div class="price">${money(o.priceMonthly)}</div>${o.sourceUrl?`<a href="${esc(o.sourceUrl)}">fonte</a>`:''}</article>`}
function renderFree(x,reader,notes,market,pipeline){const offers=(market?.offers||[]).filter(o=>Number(o.priceMonthly)>0&&Number(o.speedMbps)>0).sort((a,b)=>Number(a.priceMonthly)-Number(b.priceMonthly));const current=Number(x.internetMonthlyPrice);const best=offers[0]||null;let monthly=best&&Number.isFinite(current)?Math.max(0,current-Number(best.priceMonthly)):0;let annual=monthly*12;let decision='PRELIMINAR',message='Encontramos alternativas para a sua internet.';if(!x.bundleDetected&&pipeline?.analysis){const comp=pipeline.analysis.comparisons?.[0];if(comp){annual=Math.max(0,Number(comp.estimatedSavings12Months||annual));monthly=annual/12}decision=pipeline.finalDecision!=='ANALISE_INCONCLUSIVA'?pipeline.finalDecision:(pipeline.provisionalDecision||'PRELIMINAR');message=pipeline.message||message}else if(x.bundleDetected){message='Há oportunidade aparente na internet, mas o combo precisa ser considerado antes de recomendar uma troca final.'}const r=$('#p213Results');r.innerHTML=extractCard(x,reader,notes)+`<div class="p-real"><div class="p-status"><div><h4>Diagnóstico grátis</h4><p>${esc(message)}</p></div><span class="p-badge ${decision==='MANTENHA'?'good':''}">${esc(String(decision).replaceAll('_',' '))}</span></div><div class="p-kpis" style="margin-top:14px"><div class="p-kpi"><small>Internet atual</small><b>${money(current)}</b></div><div class="p-kpi"><small>Melhor preço anunciado</small><b>${best?money(best.priceMonthly):'—'}</b></div><div class="p-kpi"><small>Diferença bruta</small><b>${best?money(monthly):'—'}</b></div><div class="p-kpi"><small>Economia 12m</small><b>${best?money(annual):'—'}</b></div></div>${x.bundleDetected?'<div class="p-note warn">A economia acima é preliminar porque retirar a internet do combo pode alterar descontos de outros serviços.</div>':''}<div class="p-offers">${offers.slice(0,6).map(offerHidden).join('')}</div></div>`;finish()}
function friendlyError(e){if(e?.name==='AbortError')return'A análise demorou além do limite. Tente novamente com uma conexão estável.';if(e?.status===429)return'O limite gratuito do leitor foi atingido temporariamente. Tente novamente em alguns minutos.';return String(e?.message||e||'Falha inesperada na análise.')}
async function run(){if(busy)return;if(!selectedFile){alert('Escolha uma fatura primeiro.');return}if(selectedFile.size>3*1024*1024){alert('Use um arquivo de até 3 MB.');return}const mt=mime(selectedFile);if(!mt){alert('Formato não suportado. Use PDF, JPG, PNG ou WEBP.');return}busy=true;const my=++runSeq;const btn=$('#p213Analyze');if(btn){btn.disabled=true;btn.textContent='Analisando…'}resetOutput();try{setProgress(12,'Lendo sua fatura…','O Reader faz duas verificações internas no mesmo envio.','#p213c1');const payload={filename:selectedFile.name,mimeType:mt,base64:await b64(selectedFile)};const reader=await post('/api/read-bill',payload,65000);if(my!==runSeq)return;const typed=cleanCep($('#p213Cep')?.value);const rec=reconcileReader(reader,typed),x=rec.x;if(x.cep&&$('#p213Cep'))$('#p213Cep').value=x.cep;setProgress(38,'Validando os dados…','Conferindo campos críticos antes de pesquisar o mercado.','#p213c2');const critical=[];if(!x.provider)critical.push('Operadora não confirmada.');if(!(Number(x.speedMbps)>0))critical.push('Velocidade contratada não confirmada.');if(!(Number(x.internetMonthlyPrice)>0))critical.push('Preço mensal da internet não confirmado.');if(!cleanCep(x.cep||typed))critical.push('CEP não confirmado. Digite o CEP da instalação.');if(critical.length){block(x,reader,rec.notes,critical);return}let pre=null;try{pre=await post('/api/run-pipeline',{extraction:x,marketResult:{},engineConfig:{}},18000)}catch(_){pre=null}if(pre?.blockingReasons?.length){const allowed=x.bundleDetected?new Set(['BUNDLE_SERVICE_CONFIRMATION_REQUIRED','BILLING_BASELINE_UNRESOLVED','UNKNOWN_RECURRING_COMPONENTS']):new Set();const realBlocks=pre.blockingReasons.filter(b=>!allowed.has(b.code));if(realBlocks.length){block(x,reader,rec.notes,realBlocks);return}}setProgress(62,'Pesquisando ofertas…','Consultando páginas oficiais disponíveis para o seu CEP.','#p213c3');const cp=cleanCep(x.cep||typed);const mr=await post('/api/find-offers',{cep:cp,city:x.city||null,state:x.state||null},35000);if(my!==runSeq)return;let market=mr.result||{};const num=String($('#p213Number')?.value||'').trim();if(num&&market.offers?.length){setProgress(78,'Validando cobertura…','Tentando confirmar disponibilidade no endereço.','#p213c4');const providers=[...new Set(market.offers.map(o=>o.provider).filter(p=>/^(Claro|TIM|Vivo)$/i.test(String(p))))];if(providers.length){try{const ck=await post('/api/check-availability',{cep:cp,number:num,providers,marketResult:market},45000);if(ck.updatedMarket)market=ck.updatedMarket}catch(e){console.warn('availability',e)}}}else $('#p213c4')?.classList.add('done');setProgress(92,'Calculando o resultado…','Montando o diagnóstico grátis e o relatório completo bloqueado.','#p213c5');let pipeline=null;if(!x.bundleDetected){try{pipeline=await post('/api/run-pipeline',{extraction:x,marketResult:market,engineConfig:{}},18000)}catch(e){console.warn('pipeline',e)}}renderFree(x,reader,rec.notes,market,pipeline)}catch(e){console.error('Poupai V2.13',e);const r=$('#p213Results');r.innerHTML=`<div class="p-real p-block"><h4>Erro na análise</h4><p>${esc(friendlyError(e))}</p><div class="p-note">Nenhum resultado parcial ou dado demonstrativo foi utilizado.</div></div>`;finish()}finally{busy=false;if(btn){btn.disabled=false;btn.textContent='Analisar minha fatura'}}}
function install(){const shell=$('#analisar .analyzer-shell');if(!shell)return;const sec=$('#analisar .section-title p');if(sec)sec.textContent='Envie uma fatura real. O Poupai lê a conta, valida o custo da internet e pesquisa ofertas atuais para o seu CEP.';shell.innerHTML=`<div class="p213-analyzer"><div class="p213-head"><h3>Analisador Poupai</h3><span class="p213-live">🔒 Análise real • Mobile V2.13</span></div><div class="p213-body"><div class="p213-grid"><div class="p213-upload"><div><div class="p213-icon">⇧</div><h4>Envie sua fatura</h4><p>PDF, JPG, PNG ou WEBP • até 3 MB</p><button class="p213-btn" id="p213Pick" type="button">Escolher arquivo</button><input id="p213File" type="file" accept="application/pdf,image/jpeg,image/png,image/webp" hidden><div class="p213-file" id="p213FileInfo"></div></div></div><div class="p213-fields"><div class="p213-field"><label>CEP da instalação</label><input id="p213Cep" inputmode="numeric" autocomplete="postal-code" maxlength="9" placeholder="00000-000"></div><div class="p213-field"><label>Número do imóvel (opcional)</label><input id="p213Number" inputmode="text" maxlength="24" placeholder="Ex.: 120"></div><div class="p213-help">O CEP ajuda a pesquisar ofertas da região. O número é usado apenas quando conseguimos tentar validar cobertura exata.</div><button class="p213-btn primary" id="p213Analyze" type="button" style="margin-top:15px">Analisar minha fatura</button></div></div><div class="p213-progress" id="p213Progress"><h4 id="p213Title">Lendo sua fatura…</h4><p id="p213Desc">Iniciando análise.</p><div class="p213-track"><i id="p213Bar"></i></div><div class="p213-chips"><span class="p213-chip" id="p213c1">Fatura</span><span class="p213-chip" id="p213c2">Validação</span><span class="p213-chip" id="p213c3">Ofertas</span><span class="p213-chip" id="p213c4">Cobertura</span><span class="p213-chip" id="p213c5">Resultado</span></div></div><div class="p213-results" id="results"><div id="p213Results"></div></div></div></div>`;const root=shell.querySelector('.p213-analyzer');['click','change','input','drop','dragover'].forEach(ev=>root?.addEventListener(ev,e=>e.stopPropagation()));const input=$('#p213File');$('#p213Pick')?.addEventListener('click',()=>input?.click());input?.addEventListener('change',e=>{selectedFile=e.target.files?.[0]||null;const info=$('#p213FileInfo');if(selectedFile&&info){info.textContent=`${selectedFile.name} • ${(selectedFile.size/1024).toFixed(0)} KB`;info.classList.add('show')}resetOutput()});$('#p213Analyze')?.addEventListener('click',run)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install();
})();