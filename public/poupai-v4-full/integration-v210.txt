(()=>{
'use strict';
const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
const API=location.origin; let selectedFile=null;
const style=document.createElement('style');style.textContent=`
#results .result-grid,#results .compare-visual,#results #opps,#results #locked,#results #noSale,#results #report{display:none!important}
.p-real{margin-top:16px;background:#fff;border:1px solid var(--line);border-radius:20px;padding:18px}.p-real h4{margin:0 0 6px;color:var(--navy)}.p-real p{margin:0;color:var(--muted);font-size:12px}
.p-status{display:flex;justify-content:space-between;gap:12px;align-items:center}.p-badge{padding:8px 10px;border-radius:999px;font-size:11px;font-weight:950;background:#eef2f4;color:#56697d}.p-badge.good{background:#e9f9ed;color:#13772a}.p-badge.warn{background:#fff4df;color:#8c5b00}
.p-kpis{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-top:14px}.p-kpi{background:#f6f9f7;border-radius:13px;padding:12px}.p-kpi small{display:block;color:#7b8b9a;font-size:9px;text-transform:uppercase;font-weight:900}.p-kpi b{display:block;color:var(--navy);font-size:17px;margin-top:3px}
.p-offers{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-top:14px}.p-offer{border:1px solid var(--line);border-radius:15px;padding:14px;background:#fbfdfb}.p-offer h5{margin:0 0 4px;color:var(--navy)}.p-offer .price{font-size:22px;font-weight:950;color:var(--navy);margin:8px 0}.p-offer small{color:#758596}.p-offer a{display:block;margin-top:10px;background:var(--navy);color:#fff;padding:9px;border-radius:10px;text-align:center;font-size:11px;font-weight:900}.p-block{border-color:#ead8d8;background:#fffafa}.p-block ul{margin:10px 0 0;padding-left:18px;color:#6c5860;font-size:12px}.p-note{margin-top:10px;padding:10px 12px;border-radius:12px;background:#edf9ef;color:#176b2b;font-size:11px;font-weight:800}.p-note.warn{background:#fff5df;color:#8c5b00}.p-note.blue{background:#eef5ff;color:#285b91}
.cep-extra-field{margin-top:10px}.cep-extra-field label{display:block;font-size:11px;font-weight:900;color:var(--navy);margin-bottom:5px}.cep-extra-field input{width:100%;padding:12px;border:1px solid #dbe4dd;border-radius:12px}
@media(max-width:900px){.p-kpis{grid-template-columns:1fr 1fr}.p-offers{grid-template-columns:1fr}}@media(max-width:520px){.p-kpis{grid-template-columns:1fr}}
`;document.head.appendChild(style);
const money=v=>{const n=Number(v);return Number.isFinite(n)?n.toLocaleString('pt-BR',{style:'currency',currency:'BRL'}):'—'};
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const cleanCep=v=>{const d=String(v||'').replace(/\D/g,'').slice(0,8);return d.length===8?`${d.slice(0,5)}-${d.slice(5)}`:null};
const norm=s=>String(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim();
function mime(f){if(f.type)return f.type;const n=f.name.toLowerCase();if(n.endsWith('.pdf'))return'application/pdf';if(/\.jpe?g$/.test(n))return'image/jpeg';if(n.endsWith('.png'))return'image/png';if(n.endsWith('.webp'))return'image/webp';return''}
function b64(f){return new Promise((ok,no)=>{const r=new FileReader();r.onload=()=>ok(String(r.result).split(',')[1]||'');r.onerror=no;r.readAsDataURL(f)})}
async function post(path,body,timeout){const c=new AbortController(),t=setTimeout(()=>c.abort(),timeout);try{const r=await fetch(API+path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body),signal:c.signal});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.message||j.error||`HTTP ${r.status}`);return j}finally{clearTimeout(t)}}
function txt(sel,v){const e=$(sel);if(e&&v!=null)e.textContent=v}
function cleanBtn(id,fn){const o=$(id);if(!o)return;const n=o.cloneNode(true);o.replaceWith(n);n.addEventListener('click',fn)}
function phase(n,title,desc,chip){$('#uploadStage').style.display='none';$('#analysis').classList.add('show');$('#results').classList.remove('show');txt('#analysisTitle',title);txt('#analysisText',desc);if($('#progressBar'))$('#progressBar').style.width=n+'%';if(chip)$(chip)?.classList.add('done')}
function done(){if($('#progressBar'))$('#progressBar').style.width='100%';setTimeout(()=>{$('#analysis')?.classList.remove('show');$('#results')?.classList.add('show')},150)}
function clearReal(){$$('#results .p-real').forEach(e=>e.remove());$$('.chip').forEach(e=>e.classList.remove('done'))}
function sameNum(a,b,tol=.02){const x=Number(a),y=Number(b);return Number.isFinite(x)&&Number.isFinite(y)&&Math.abs(x-y)<=tol}
function canonicalProvider(v){const n=norm(v);if(n.includes('claro'))return'Claro';if(/(^| )tim( |$)/.test(n))return'TIM';if(n.includes('vivo')||n.includes('telefonica'))return'Vivo';if(n.includes('brisanet'))return'Brisanet';if(n.includes('unifique'))return'Unifique';if(n.includes('desktop'))return'Desktop';if(/(^| )nio( |$)/.test(n))return'Nio';return String(v||'').trim()||null}
function priceEvidenceScore(r){const e=norm(r?.evidence?.internetMonthlyPrice);let s=0;if(/subtotal.*(fibra|internet)|(fibra|internet).*subtotal/.test(e))s+=10;if(e.includes('plano contratado'))s+=5;if(e.includes('vivo fibra')||e.includes('internet'))s+=2;if(e.includes('scm'))s-=8;if(e.includes('valor base')||e.includes('itens da fatura')||e.includes('nfcom'))s-=5;return s}
function reconcileInternetPrice(a,b,provider){
 if(a.internetMonthlyPrice!=null&&b.internetMonthlyPrice!=null&&sameNum(a.internetMonthlyPrice,b.internetMonthlyPrice,.05))return{value:Number(a.internetMonthlyPrice),reason:null};
 const pa=Number(a.internetMonthlyPrice),pb=Number(b.internetMonthlyPrice),sa=priceEvidenceScore(a),sb=priceEvidenceScore(b);
 if(Number.isFinite(pa)&&Number.isFinite(pb)){
   if(sa>=sb+4)return{value:pa,reason:'Preço comercial da internet prevaleceu sobre componente contábil/fiscal.'};
   if(sb>=sa+4)return{value:pb,reason:'Preço comercial da internet prevaleceu sobre componente contábil/fiscal.'};
   const vivo=provider==='Vivo';const sameSpeed=sameNum(a.speedMbps,b.speedMbps,1);const bundle=Boolean(a.bundleDetected||b.bundleDetected);const hi=Math.max(pa,pb),lo=Math.min(pa,pb),gap=hi-lo;
   if(vivo&&sameSpeed&&bundle&&hi>=70&&hi<=220&&lo>=50&&gap>=10&&gap<=60){return{value:hi,reason:'Vivo Total: reconciliado subtotal comercial da Fibra com a linha SCM da nota fiscal.'}}
 }
 return{value:null,reason:null};
}
function mergeReads(a,b,typedCep){
 const x={...a},issues=[],notes=[];const pa=canonicalProvider(a.provider),pb=canonicalProvider(b.provider);if(pa&&pb&&norm(pa)===norm(pb))x.provider=pa;else{x.provider=null;issues.push('As duas leituras não concordaram sobre a operadora.')}
 if(a.speedMbps!=null&&b.speedMbps!=null&&sameNum(a.speedMbps,b.speedMbps,1))x.speedMbps=Number(a.speedMbps);else{x.speedMbps=null;issues.push('As duas leituras não concordaram sobre a velocidade contratada.')}
 const pr=reconcileInternetPrice(a,b,x.provider||pa||pb);x.internetMonthlyPrice=pr.value;if(pr.reason)notes.push(pr.reason);if(x.internetMonthlyPrice==null)issues.push('As duas leituras não permitiram confirmar o preço mensal comercial da internet.')
 if(a.invoiceTotal!=null&&b.invoiceTotal!=null&&sameNum(a.invoiceTotal,b.invoiceTotal,.05))x.invoiceTotal=Number(a.invoiceTotal);else{x.invoiceTotal=null;issues.push('As duas leituras não concordaram sobre o total da fatura.')}
 const ca=cleanCep(a.cep),cb=cleanCep(b.cep);if(typedCep)x.cep=typedCep;else if(ca&&cb&&ca===cb)x.cep=ca;else{x.cep=null;if(ca||cb)issues.push('As duas leituras não concordaram sobre o CEP.')}
 x.city=norm(a.city)&&norm(a.city)===norm(b.city)?a.city:null;x.state=norm(a.state)&&norm(a.state)===norm(b.state)?a.state:null;
 x.bundleDetected=Boolean(a.bundleDetected||b.bundleDetected);x.internetPriceIsolated=Boolean((a.internetPriceIsolated||b.internetPriceIsolated)&&x.internetMonthlyPrice>0);
 x.planName=norm(a.planName)&&norm(b.planName)&&(norm(a.planName)===norm(b.planName)||norm(a.planName).includes(norm(b.planName))||norm(b.planName).includes(norm(a.planName)))?(String(a.planName).length<=String(b.planName).length?a.planName:b.planName):(x.speedMbps?`${x.provider||''} Fibra ${x.speedMbps} Mbps`.trim():null);
 const key=e=>`${norm(e?.name)}|${Number(e?.price??-1).toFixed(2)}|${e?.category||''}`;const bkeys=new Set((b.extras||[]).map(key));x.extras=(a.extras||[]).filter(e=>bkeys.has(key(e)));
 x.confidence={...(a.confidence||{})};for(const k of ['provider','internetMonthlyPrice','invoiceTotal','speedMbps','cep']){x.confidence[k]=Math.min(Number(a.confidence?.[k]||0),Number(b.confidence?.[k]||0),.95)}if(!x.provider)x.confidence.provider=0;if(!x.speedMbps)x.confidence.speedMbps=0;if(!x.internetMonthlyPrice)x.confidence.internetMonthlyPrice=0;if(!x.invoiceTotal)x.confidence.invoiceTotal=0;if(!x.cep&&!typedCep)x.confidence.cep=0;if(typedCep)x.confidence.cep=1;x.confidence.overall=Math.min(Number(a.confidence?.overall||0),Number(b.confidence?.overall||0),.95);
 return{extraction:x,issues,notes};
}
function renderExtract(x,issues,notes){txt('#xProvider',x.provider||'Não identificado');txt('#xService',x.bundleDetected?'Internet (no combo)':'Internet');txt('#xPlan',x.speedMbps?`${x.speedMbps} Mbps`:'Não identificado');txt('#xBill',money(x.invoiceTotal));txt('#xAdds',x.bundleDetected?'Combo identificado':`${Array.isArray(x.extras)?x.extras.length:0} confirmado(s)`);const v=$('.extracted .verified');if(v)v.textContent=issues?.length?'⚠ leitura com divergência':'✓ dupla leitura validada';const box=document.createElement('div');box.className='p-real';box.innerHTML=`<h4>Valor usado para comparar a internet</h4><div class="p-kpis"><div class="p-kpi"><small>Internet recorrente</small><b>${money(x.internetMonthlyPrice)}</b></div><div class="p-kpi"><small>Fatura total</small><b>${money(x.invoiceTotal)}</b></div><div class="p-kpi"><small>Velocidade</small><b>${x.speedMbps?`${x.speedMbps} Mbps`:'—'}</b></div><div class="p-kpi"><small>Tipo</small><b>${x.bundleDetected?'Combo':'Internet isolada'}</b></div></div>${notes?.length?`<div class="p-note blue">${notes.map(esc).join(' ')}</div>`:''}`;$('.extracted')?.insertAdjacentElement('afterend',box)}
function block(items,title='Análise inconclusiva'){const b=document.createElement('div');b.className='p-real p-block';b.innerHTML=`<h4>${esc(title)}</h4><p>O Poupai preferiu parar a comparação a completar dados que não ficaram comprovados.</p><ul>${(items||[]).map(x=>`<li>${esc(x.message||x.code||x)}</li>`).join('')}</ul>`;$('.extracted')?.insertAdjacentElement('afterend',b)}
function offerCard(o,i,current){const p=Number(o.priceMonthly);const diff=Number.isFinite(p)?current-p:null;const confirmed=o.availabilityConfirmed===true||o.availability==='confirmed';return `<article class="p-offer"><small>#${i+1} • ${confirmed?'cobertura confirmada':'confirmar cobertura'}</small><h5>${esc(o.provider||'Operadora')} • ${esc(o.planName||'Plano')}</h5><div>${o.speedMbps||'—'} Mbps${o.technology?' • '+esc(o.technology):''}</div><div class="price">${money(o.priceMonthly)} <small>/mês anunciado</small></div><small>${diff==null?'Diferença não calculada':diff>0?`Até ${money(diff)}/mês abaixo do componente internet atual`:`${money(Math.abs(diff))}/mês acima do componente internet atual`}</small>${o.sourceUrl?`<a href="${esc(o.sourceUrl)}" target="_blank" rel="noopener">Ver fonte oficial ↗</a>`:''}</article>`}
function renderBundleMarket(x,market){const offers=(market?.offers||[]).filter(o=>Number(o.speedMbps)>0&&Number(o.priceMonthly)>0).sort((a,b)=>Number(a.priceMonthly)-Number(b.priceMonthly));const current=Number(x.internetMonthlyPrice);const best=offers[0]||null;const monthly=best?Math.max(0,current-Number(best.priceMonthly)):0;const b=document.createElement('div');b.className='p-real';b.innerHTML=`<div class="p-status"><div><h4>Comparação preliminar da internet</h4><p>A Fibra foi isolada em ${money(current)}, mas sua conta é Vivo Total. O impacto no preço do celular ainda precisa ser confirmado antes de recomendar troca.</p></div><span class="p-badge warn">PRELIMINAR</span></div><div class="p-kpis"><div class="p-kpi"><small>Internet atual</small><b>${money(current)}</b></div><div class="p-kpi"><small>Melhor preço anunciado</small><b>${best?money(best.priceMonthly):'—'}</b></div><div class="p-kpi"><small>Diferença bruta</small><b>${best?money(monthly):'—'}</b></div><div class="p-kpi"><small>Economia anual bruta</small><b>${best?money(monthly*12):'—'}</b></div></div><div class="p-note warn">Não trate essa economia como líquida ainda: cancelar ou migrar a Fibra pode alterar descontos/condições do Vivo Pós Família. O Poupai precisa confirmar essa dependência antes de emitir TROQUE/MANTENHA.</div>${offers.length?`<div class="p-offers">${offers.slice(0,6).map((o,i)=>offerCard(o,i,current)).join('')}</div>`:'<div class="p-note">Nenhuma oferta comparável foi extraída das páginas oficiais consultadas.</div>'}`;$('.extracted')?.insertAdjacentElement('afterend',b)}
function renderNormalResult(p){const a=p.analysis;if(!a){block([{message:p.message||'Sem dados suficientes para comparar.'}]);return}const comps=a.comparisons||[],best=comps[0]||null,final=p.finalDecision!=='ANALISE_INCONCLUSIVA',decision=final?p.finalDecision:(p.provisionalDecision||'ANALISE_INCONCLUSIVA');const s12=best?Math.max(0,Number(best.estimatedSavings12Months||0)):0,s24=best?Math.max(0,Number(best.estimatedSavings24Months||0)):0;const b=document.createElement('div');b.className='p-real';b.innerHTML=`<div class="p-status"><div><h4>${final?'Recomendação final':'Análise preliminar'}</h4><p>${esc(p.message||'Resultado calculado com esta fatura.')}</p></div><span class="p-badge ${final?'good':'warn'}">${esc(decision.replaceAll('_',' '))}</span></div><div class="p-kpis"><div class="p-kpi"><small>Custo atual</small><b>${money(p.billingBaseline?.baselineMonthlyCost)}</b></div><div class="p-kpi"><small>Economia 12m</small><b>${money(s12)}</b></div><div class="p-kpi"><small>Economia 24m</small><b>${money(s24)}</b></div><div class="p-kpi"><small>Ofertas válidas</small><b>${comps.length}</b></div></div>`;$('.extracted')?.insertAdjacentElement('afterend',b)}
async function run(){if(!selectedFile)selectedFile=$('#fileInput')?.files?.[0]||null;if(!selectedFile){alert('Escolha uma fatura.');return}if(selectedFile.size>3*1024*1024){alert('Arquivo acima de 3 MB.');return}const mt=mime(selectedFile);if(!mt){alert('Formato não suportado.');return}clearReal();try{const data=await b64(selectedFile);phase(10,'Lendo sua fatura…','Primeira leitura independente do documento.','#c1');const r1=await post('/api/read-bill',{filename:selectedFile.name,mimeType:mt,base64:data},60000);phase(24,'Conferindo a leitura…','Executando uma segunda leitura independente.','#c2');const r2=await post('/api/read-bill',{filename:selectedFile.name,mimeType:mt,base64:data},60000);const typed=cleanCep($('#cep')?.value),m=mergeReads(r1.extraction||{},r2.extraction||{},typed),x=m.extraction;if(x.cep&&$('#cep'))$('#cep').value=x.cep;renderExtract(x,m.issues,m.notes);const critical=[];if(!x.provider)critical.push('Operadora não confirmada.');if(!(Number(x.speedMbps)>0))critical.push('Velocidade contratada não confirmada.');if(!(Number(x.internetMonthlyPrice)>0))critical.push('Preço comercial da internet não confirmado.');if(critical.length){done();block([...critical,...m.issues]);return}const cp=cleanCep(x.cep||$('#cep')?.value);if(!cp){done();block(['CEP não confirmado. Digite o CEP da instalação e analise novamente.']);return}phase(58,'Pesquisando ofertas…','Consultando páginas oficiais disponíveis.','#c3');const mr=await post('/api/find-offers',{cep:cp,city:x.city||null,state:x.state||null},25000);let market=mr.result||{};const num=String($('#houseNumber')?.value||'').trim();if(num&&market.offers?.length){phase(76,'Validando cobertura…','Tentando confirmar o endereço.','#c4');const providers=[...new Set(market.offers.map(o=>o.provider).filter(p=>/^(Claro|TIM|Vivo)$/i.test(String(p))))];if(providers.length){try{const ck=await post('/api/check-availability',{cep:cp,number:num,providers,marketResult:market},45000);if(ck.updatedMarket)market=ck.updatedMarket}catch(e){console.warn(e)}}}else $('#c4')?.classList.add('done');if(x.bundleDetected&&x.internetPriceIsolated){phase(92,'Montando comparação preliminar…','Separando o componente internet sem fingir que o combo inteiro custa isso.','#c5');done();renderBundleMarket(x,market);return}phase(90,'Calculando a recomendação…','Aplicando o motor completo à conta validada.','#c5');const p=await post('/api/run-pipeline',{extraction:x,marketResult:market,engineConfig:{}},15000);done();if(p.blockingReasons?.length){block(p.blockingReasons);return}renderNormalResult(p)}catch(e){console.error(e);done();block([e?.name==='AbortError'?'A análise demorou mais que o esperado.':String(e?.message||e)],'Falha na análise')}}
function install(){const demo=$('.demo-bar');if(demo)demo.innerHTML='<b>Análise real V2.10</b> • dupla leitura • reconciliação de faturas combo';txt('.secure-pill','🔒 Análise real • V2.10');txt('#analisar .section-title p','Envie uma fatura real. O Poupai separa o custo da internet, pesquisa ofertas oficiais e sinaliza quando um combo exige confirmação adicional.');$('.demo-scenarios')?.setAttribute('style','display:none!important');if($('#cep')){$('#cep').value='';$('#cep').placeholder='00000-000'}if($('.cep-box')&&!$('#houseNumber')){const w=document.createElement('div');w.className='cep-extra-field';w.innerHTML='<label>Número do imóvel (opcional)</label><input id="houseNumber" maxlength="24" placeholder="Ex.: 120">';$('.cep-box').appendChild(w)}const n=document.createElement('div');n.className='p-note';n.textContent='O Poupai distingue preço comercial do pacote e componentes contábeis como SCM/SVA. Em combos, a comparação da internet é mostrada como preliminar até validar impacto nos outros serviços.';$('.cep-box')?.appendChild(n);$('#fileInput')?.addEventListener('change',e=>selectedFile=e.target.files?.[0]||null);$('#drop')?.addEventListener('drop',e=>selectedFile=e.dataTransfer?.files?.[0]||null);cleanBtn('#pickBtn',()=>$('#fileInput')?.click());cleanBtn('#analyzeBtn',run)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();
})();
