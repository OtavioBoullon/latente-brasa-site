ção disponível para migrar meu plano sem reduzir a entrega. Antes de confirmar, preciso do valor final, duração da promoção, fidelidade, multa, instalação e preço após o período promocional.');
  set('metaProvider',s.provider);set('metaConfidence',s.confidence);set('methodConfidence',confidenceNum+'% confiança');
}
$('#unlockBtn').onclick=()=>$('#payModal').classList.add('show');$('#closeModal').onclick=()=>$('#payModal').classList.remove('show');$('#payModal').addEventListener('click',e=>{if(e.target.id==='payModal')e.currentTarget.classList.remove('show')});$$('.method').forEach(x=>x.onclick=()=>{$$('.method').forEach(m=>m.classList.remove('active'));x.classList.add('active')});
$('#confirmPay').onclick=()=>{$('#payModal').classList.remove('show');$('#locked').style.display='none';$('#report').classList.add('show');toast();setTimeout(()=>$('#report').scrollIntoView({behavior:'smooth',block:'start'}),120)};
function toast(){let t=$('#toast');t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2400)}
function reset(){input.value='';picked.classList.remove('show');$('#uploadStage').style.display='block';$('#analysis').classList.remove('show');$('#results').classList.remove('show');$('#report').classList.remove('show');$('#locked').style.display='block';$('#noSale').classList.remove('show');$('#progressBar').style.width='0';$$('.chip').forEach(x=>x.classList.remove('done'));$$('.demo-scenarios button').forEach(x=>x.classList.remove('active'));$('#analisar').scrollIntoView({behavior:'smooth'})}
$('#resetBtn').onclick=reset;$('#noSaleReset').onclick=reset;$('#printBtn').onclick=()=>window.print();
$('#copyScriptBtn').onclick=async()=>{try{await navigator.clipboard.writeText($('#negotiationScript').textContent);$('#copyScriptBtn').textContent='Copiado ✓';setTimeout(()=>$('#copyScriptBtn').textContent='Copiar texto',1600)}catch(e){toast()}};
$('#copySummaryBtn').onclick=async()=>{const s=currentScenario,monthly=Math.max(0,s.bill-s.target);const txt='Poupai — Resumo do diagnóstico\nConta atual: '+fmt(s.bill)+'\nCenário recomendado: '+fmt(s.target)+'\nEconomia mensal estimada: '+fmt(monthly)+'\nEconomia em 12 meses: '+fmt(monthly*12)+'\nPoupai Score: '+s.score+'/100\nConfiança: '+s.confidence;try{await navigator.clipboard.writeText(txt);$('#copySummaryBtn').textContent='Resumo copiado ✓';setTimeout(()=>$('#copySummaryBtn').textContent='Copiar resumo',1600)}catch(e){toast()}};
$$('.faq-q').forEach(q=>q.onclick=()=>q.parentElement.classList.toggle('open'));


// ===== POUPAI V4 — interações adicionais =====
function parseBR(v){if(typeof v==='number')return v;return parseFloat(String(v||'0').replace(/\./g,'').replace(',','.'))||0}
function currentConfidence(){return parseInt((currentScenario.confidence.match(/\d+/)||['80'])[0])}
function updateV4Dynamic(){
  const s=currentScenario,r=s.report,conf=currentConfidence();
  const mainCurrent=r[0], mainAlt=r[1], addsCurrent=r[2], addsAlt=r[3], addSaving=Math.max(0,addsCurrent-addsAlt);
  const set=(id,val)=>{const e=$('#'+id);if(e)e.textContent=val};
  set('evMain',fmt(mainCurrent)+' '); set('evAdd',fmt(addSaving)+' '); set('evPlan',s.plan+' '); set('evFile',$('#fileName')?$('#fileName').textContent:'fatura.pdf');
  // Recreate evidence buttons if textContent replacement removed them
  const traceDoc=$('.trace-doc'); if(traceDoc){const lines=traceDoc.querySelectorAll('.trace-line b'); const defs=[['main',fmt(mainCurrent)],['addon',fmt(addSaving)],['plan',s.plan]]; defs.forEach((d,i)=>{if(lines[i])lines[i].innerHTML=d[1]+' <button class="evidence-btn" data-evidence="'+d[0]+'">ver trecho</button>'});}
  set('traceCep',$('#cep')?.value||'CEP não informado'); set('evFile',$('#fileName')?.textContent||'fatura.pdf');
  const now=new Date(); set('traceTime','estrutura demo · '+now.toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'}));
  set('confTotal',conf+'%'); const offer=Math.min(96,Math.max(72,conf+4)); const cep=Math.min(94,Math.max(70,conf-4)); set('confOffer',offer+'%');set('confCep',cep+'%'); if($('#confOfferBar'))$('#confOfferBar').style.width=offer+'%';if($('#confCepBar'))$('#confCepBar').style.width=cep+'%';
  set('strategyIdeal','≤ '+fmt(s.target)+'/mês'); const acceptable=(s.target+s.bill)/2; set('strategyAccept','≤ '+fmt(acceptable)+'/mês');set('strategyGoal','meta ≤ '+fmt(s.target).replace(',00',''));set('strategyLimit','limite ≤ '+fmt(acceptable).replace(',00',''));set('strategyKeep',s.plan+' / qualidade');set('strategyVerdict',s.score>=75?'Comece tentando reduzir o preço sem trocar de operadora. Se a proposta não entrar na faixa aceitável, use a concorrência como segunda etapa.':s.score>=50?'Primeiro revise condições e tente uma redução simples. A troca só vale se o ganho líquido continuar relevante após fidelidade e taxas.':'A conta já está relativamente competitiva; evite trocar apenas por uma diferença pequena.');
  updateWhatIf();
}
function updateWhatIf(){const s=currentScenario,r=s.report; if(!$('#wfStay'))return; const stay=$('#wfStay').checked, loyalty=$('#wfLoyalty').checked, addons=$('#wfAddons').checked, sw=$('#wfSwitch').checked; const mainAlt=r[1], addCurrent=r[2], addAlt=r[3], addSaving=Math.max(0,addCurrent-addAlt), competitorSaving=Math.max(0,r[8]||0); let target=s.bill; let change='Muito baixo',desc=[]; if(stay){target=Math.min(target,mainAlt+(addons?addAlt:addCurrent));desc.push('permanecer na operadora')} if(addons&&addSaving>0){target=Math.min(target,s.bill-addSaving);desc.push('revisar adicionais')} if(sw){target=Math.min(target,Math.max(0,r[0]-competitorSaving)+(addons?addAlt:addCurrent));change='Alto';desc.push('permitir troca')} else if(stay){change='Baixo'} if(!loyalty){target+=Math.min(15,s.bill*.07);desc.push('evitar fidelidade')} target=Math.min(s.bill,Math.max(0,target)); const m=Math.max(0,s.bill-target); $('#wfHeadline').textContent=fmt(target)+'/mês';$('#wfMonthly').textContent=fmt(m);$('#wfYear').textContent=fmt(m*12);$('#wfChange').textContent=change;$('#wfText').textContent='Prioridade: '+(desc.length?desc.join(', '):'manter configuração atual')+'.';$('#wfWarning').textContent=!loyalty?'Sem fidelidade, algumas ofertas deixam de ser elegíveis e a economia potencial diminui.':'O simulador continua penalizando taxas e condições não confirmadas.'}
['wfStay','wfLoyalty','wfAddons','wfSwitch'].forEach(id=>{const e=$('#'+id);if(e)e.addEventListener('change',updateWhatIf)});
function analyzeProposal(){const s=currentScenario;const p=parseBR($('#propPrice').value), loyalty=Math.max(0,parseInt($('#propLoyalty').value)||0),install=parseBR($('#propInstall').value),after=parseBR($('#propAfter').value)||p,promo=Math.min(12,Math.max(0,parseInt($('#propPromoMonths').value)||12));const cost=p*promo+after*(12-promo)+install;const current=s.bill*12,best=s.target*12,save=current-cost,diff=cost-best;let cls='bad',label='não recomendado',title='Esta proposta ainda não bate o cenário recomendado',text='O custo total em 12 meses ficou acima da referência que o Poupai considera mais eficiente.';if(save>0&&diff<=Math.max(60,best*.05)){cls='good';label='boa proposta';title='A proposta está dentro da faixa recomendada';text='O custo total é competitivo e a economia se mantém quando instalação e preço pós-promoção entram na conta.'}else if(save>0){cls='warn';label='negociar mais';title='Existe economia, mas ainda há espaço para melhorar';text='A proposta reduz sua conta, porém continua mais cara que o cenário-base. Use a referência do relatório para tentar uma condição melhor.'} if(loyalty>12){text+=' Atenção: a fidelidade informada é superior a 12 meses nesta simulação e merece revisão.'} const st=$('#proposalStatus');st.className='proposal-status '+cls;st.textContent=label;$('#proposalTitle').textContent=title;$('#proposalText').textContent=text;$('#prop12').textContent=fmt(cost);$('#propSave').textContent=(save>=0?'+ ':'- ')+fmt(Math.abs(save));$('#propVsBest').textContent=(diff>=0?'+ ':'- ')+fmt(Math.abs(diff))}
if($('#analyzeProposalBtn'))$('#analyzeProposalBtn').onclick=analyzeProposal;
function openEvidence(type){const s=currentScenario,r=s.report;const file=$('#fileName')?.textContent||'fatura_demo.pdf';let line='',title='Evidência da fatura';if(type==='main')line='Plano principal / '+s.plan+' ........ <span class="evidence-highlight">'+fmt(r[0])+'</span>';else if(type==='addon')line='Serviço adicional revisável ........ <span class="evidence-highlight">'+fmt(Math.max(0,r[2]-r[3]))+'</span>';else line='Velocidade / plano ........ <span class="evidence-highlight">'+s.plan+'</span>';$('#evidenceModalTitle').textContent=title;$('#evidencePreview').innerHTML='Documento: '+file+'<br>Página/linha: exemplo de estrutura<br>'+line;$('#evidenceOrigin').textContent='Fatura enviada';$('#evidenceStatus').textContent='Trecho demonstrativo';$('#evidenceCep').textContent=$('#cep')?.value||'—';$('#evidenceWhen').textContent='Durante a análise';$('#evidenceModal').classList.add('show')}
function openSource(name){$('#evidenceModalTitle').textContent='Ficha da fonte — '+name;$('#evidencePreview').innerHTML='Fonte prevista: <span class="evidence-highlight">canal oficial da operadora</span><br>Oferta: '+name+'<br>Preço / condições: registrados pelo motor no momento da consulta<br>Elegibilidade: validada por CEP antes de classificar como disponível';$('#evidenceOrigin').textContent='Canal oficial (estrutura futura)';$('#evidenceStatus').textContent='Simulação de rastreabilidade';$('#evidenceCep').textContent=$('#cep')?.value||'—';$('#evidenceWhen').textContent='Timestamp será armazenado';$('#evidenceModal').classList.add('show')}
document.addEventListener('click',e=>{const eb=e.target.closest('.evidence-btn');if(eb)openEvidence(eb.dataset.evidence);const sb=e.target.closest('.source-open');if(sb)openSource(sb.dataset.source||'Oferta')});
if($('#closeEvidenceModal'))$('#closeEvidenceModal').onclick=()=>$('#evidenceModal').classList.remove('show');if($('#evidenceModal'))$('#evidenceModal').addEventListener('click',e=>{if(e.target.id==='evidenceModal')e.currentTarget.classList.remove('show')});
function saveReport(){const s=currentScenario;let arr=JSON.parse(localStorage.getItem('poupaiReports')||'[]');arr.unshift({id:$('#reportCode')?.textContent||'PP-DEMO',provider:s.provider,bill:s.bill,target:s.target,date:new Date().toISOString()});arr=arr.slice(0,10);localStorage.setItem('poupaiReports',JSON.stringify(arr));updateSavedCount();toastMsg('Relatório salvo na Minha área.')}
function updateSavedCount(){let arr=[];try{arr=JSON.parse(localStorage.getItem('poupaiReports')||'[]')}catch(e){} if($('#savedReportCount'))$('#savedReportCount').textContent=arr.length}
function toastMsg(msg){const t=$('#toast');if(!t)return;t.textContent=msg;t.classList.add('show');setTimeout(()=>{t.classList.remove('show');t.textContent='Relatório desbloqueado.'},2400)}
if($('#saveReportBtn'))$('#saveReportBtn').onclick=saveReport;
if($('#shareReportBtn'))$('#shareReportBtn').onclick=async()=>{const s=currentScenario;const text='Poupai — diagnóstico: conta '+fmt(s.bill)+', cenário '+fmt(s.target)+', potencial anual '+fmt(Math.max(0,s.bill-s.target)*12)+'.';try{if(navigator.share)await navigator.share({title:'Meu diagnóstico Poupai',text});else{await navigator.clipboard.writeText(text);toastMsg('Resumo para compartilhar copiado.')}}catch(e){}};
if($('#openSavedBtn'))$('#openSavedBtn').onclick=()=>{let arr=[];try{arr=JSON.parse(localStorage.getItem('poupaiReports')||'[]')}catch(e){} toastMsg(arr.length?arr.length+' relatório(s) salvo(s) neste navegador.':'Você ainda não salvou nenhum relatório.')};
function renderFeedback(){let arr=[];try{arr=JSON.parse(localStorage.getItem('poupaiFeedback')||'[]')}catch(e){} const box=$('#userFeedbackList');if(!box)return;box.innerHTML=arr.slice(0,3).map(x=>{const rating=Math.min(5,Math.max(1,Number(x.rating)||5));const stars='★'.repeat(rating)+'☆'.repeat(5-rating);const meta=[x.city,x.service].filter(Boolean).map(escapeHtml).join(' · ');return '<div class="user-feedback"><div class="user-feedback-head"><div><b>'+escapeHtml(x.name)+' · avaliação local</b><small>'+meta+'</small></div><span class="uf-stars">'+stars+'</span></div><p>'+escapeHtml(x.text)+(x.saving?' <span class="uf-saving">· '+escapeHtml(x.saving)+'</span>':'')+'</p></div>'}).join('')}
function escapeHtml(v){return String(v||'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
if($('#starInput'))$$('#starInput button').forEach(btn=>btn.addEventListener('click',()=>{const n=Number(btn.dataset.star);$('#feedbackRating').value=String(n);$$('#starInput button').forEach((b,i)=>b.classList.toggle('on',i<n))}));
if($('#feedbackForm'))$('#feedbackForm').addEventListener('submit',e=>{e.preventDefault();let arr=[];try{arr=JSON.parse(localStorage.getItem('poupaiFeedback')||'[]')}catch(x){}arr.unshift({name:$('#feedbackName').value.trim()||'Cliente beta',city:$('#feedbackCity').value.trim(),service:$('#feedbackService').value,rating:Number($('#feedbackRating').value)||5,saving:$('#feedbackSaving').value.trim(),text:$('#feedbackText').value.trim()});localStorage.setItem('poupaiFeedback',JSON.stringify(arr.slice(0,5)));e.target.reset();$('#feedbackRating').value='5';$$('#starInput button').forEach(b=>b.classList.add('on'));renderFeedback();toastMsg('Avaliação salva localmente. Obrigado!')});
const oldRenderScenario=renderScenario;renderScenario=function(){oldRenderScenario();updateV4Dynamic()};
const oldRenderReport=renderReport;renderReport=function(s){oldRenderReport(s);setTimeout(updateV4Dynamic,0)};
updateSavedCount();renderFeedback();

</script>
</body></html>