(()=>{
'use strict';
const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
const API=location.origin; let selectedFile=null;
const style=document.createElement('style');style.textContent=`
#results .result-grid,#results .compare-visual,#results #opps,#results #locked,#results #noSale,#results #report{display:none!important}
.p-real{margin-top:16px;background:#fff;border:1px solid var(--line);border-radius:20px;padding:18px}.p-real h4{margin:0 0 6px;color:var(--navy)}.p-real p{margin:0;color:var(--muted);font-size:12px}
.p-status{display:flex;justify-content:space-between;gap:12px;align-items:center}.p-badge{padding:8px 10px;border-radius:999px;font-size:11px;font-weight:950;background:#eef2f4;color:#56697d}.p-badge.good{background:#e9f9ed;color:#13772a}.p-badge.warn{background:#fff4df;color:#8c5b00}
.p-kpis{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-top:14px}.p-kpi{background:#f6f9f7;border-radius:13px;padding:12px}.p-kpi small{display:block;color:#7b8b9a;font-size:9px;text-transform:uppercase;font-weight:900}.p-kpi b{display:block;color:var(--navy);font-size:17px;margin-top:3px}
.p-offers{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-top:14px}.p-offer{border:1px solid var(--line);border-radius:15px;padding:14px;background:#fbfdfb}.p-offer h5{margin:0 0 4px;color:var(--navy)}.p-offer .price{font-size:22px;font-weight:950;color:var(--navy);margin:8px 0}.p-offer small{color:#758596}.p-offer a{display:block;margin-top:10px;background:var(--navy);color:#fff;padding:9px;border-radius:10px;text-align:center;font-size:11px;font-weight:900}.p-block{border-color:#ead8d8;background:#fffafa}.p-block ul{margin:10px 0 0;padding-left:18px;color:#6c5860;font-size:12px}.p-note{margin-top:10px;padding:10px 12px;border-radius:12px;background:#edf9ef;color:#176b2b;font-size:11px;font-weight:800}.p-note.warn{background:#fff5df;color:#8c5b00}
.cep-extra-field{margin-top:10px}.cep-extra-field label{display:block;font-size:11px;font-weight:900;color:var(--navy);margin-bottom:5px}.cep-extra-field input{width:100%;padding:12px;border:1px solid #dbe4dd;border-radius:12px}
@media(max-width:800px){.p-offers{grid-template-columns:1fr}.p-kpis{grid-template-columns:1fr}}
`;document.head.appendChild(style);
const money=v=>{const n=Number(v);return Number.isFinite(n)?n.toLocaleString('pt-BR',{style:'currency',currency:'BRL'}):'—'};
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const cleanCep=v=>{const d=String(v||'').replace(/\D/g,'').slice(0,8);return d.length===8?`${d.slice(0,5)}-${d.slice(5)}`:null};
const norm=s=>String(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim();
function mime(f){if(f.type)return f.type;const n=f.name.toLowerCase();if(n.endsWith('.pdf'))return'application/pdf';if(/\.jpe?g$/.test(n))return'image/jpeg';if(n.endsWith('.png'))return'image/png';if(n.endsWith('.webp'))return'image/webp';return''}
function b64(f){return new Promise((ok,no)=>{const r=new FileReader();r.onload=()=>ok(String(r.result).split(',')[1]||'');r.onerror=no;r.readAsDataURL(f)})}
async function post(path,body,timeout){const c=new AbortController(),t=setTimeout(()=>c.abort(),timeout);try{const r=await fetch(API+path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body),signal:c.signal});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.message||j.error||`HTTP ${r.status}`);return j}finally{clearTimeout(t)}}
function txt(id,v){const e=$(id);if(e&&v!=null)e.textContent=v}
function cleanBtn(id,fn){const o=$(id);if(!o)return;const n=o.cloneNode(true);o.replaceWith(n);n.addEventListener('click',fn)}
function phase(n,title,desc,chip){$('#uploadStage').style.display='none';$('#analysis').classList.add('show');$('#results').classList.remove('show');txt('#analysisTitle',title);txt('#analysisText',desc);if($('#progressBar'))$('#progressBar').style.width=n+'%';if(chip)$(chip)?.classList.add('done')}
function done(){if($('#progressBar'))$('#progressBar').style.width='100%';setTimeout(()=>{$('#analysis')?.classList.remove('show');$('#results')?.classList.add('show')},150)}
function clearReal(){$$('#results .p-real').forEach(e=>e.remove());$$('.chip').forEach(e=>e.classList.remove('done'))}
function sameNum(a,b,tol=.02){const x=Number(a),y=Number(b);return Number.isFinite(x)&&Number.isFinite(y)&&Math.abs(x-y)<=tol}
function canonicalProvider(v){const n=norm(v);if(n.includes('claro'))return'Claro';if(/(^| )tim( |$)/.test(n))return'TIM';if(n.includes('vivo')||n.includes('telefonica'))return'Vivo';if(n.includes('brisanet'))return'Brisanet';if(n.includes('unifique'))return'Unifique';if(n.includes('desktop'))return'Desktop';if(/(^| )nio( |$)/.test(n))return'Nio';return String(v||'').trim()||null}
function conservativeMerge(a,b,typedCep){
 const x={...a}; const issues=[];
 const pa=canonicalProvider(a.provider),pb=canonicalProvider(b.provider); if(pa&&pb&&norm(pa)===norm(pb))x.provider=pa;else{x.provider=null;issues.push('As duas leituras não concordaram sobre a operadora.')}
 if(a.speedMbps!=null&&b.speedMbps!=null&&sameNum(a.speedMbps,b.speedMbps,1))x.speedMbps=Number(a.speedMbps);else{x.speedMbps=null;issues.push('As duas leituras não concordaram sobre a velocidade contratada.')}
 if(a.internetMonthlyPrice!=null&&b.internetMonthlyPrice!=null&&sameNum(a.internetMonthlyPrice,b.internetMonthlyPrice,.05))x.internetMonthlyPrice=Number(a.internetMonthlyPrice);else{x.internetMonthlyPrice=null;issues.push('As duas leituras não concordaram sobre o preço mensal da internet.')}
 if(a.invoiceTotal!=null&&b.invoiceTotal!=null&&sameNum(a.invoiceTotal,b.invoiceTotal,.05))x.invoiceTotal=Number(a.invoiceTotal);else{x.invoiceTotal=null;issues.push('As duas leituras não concordaram sobre o total da fatura.')}
 const ca=cleanCep(a.cep),cb=cleanCep(b.cep);if(typedCep)x.cep=typedCep;else if(ca&&cb&&ca===cb)x.cep=ca;else{x.cep=null;if(ca||cb)issues.push('As duas leituras não concordaram sobre o CEP.')}
 const planA=norm(a.planName),planB=norm(b.planName);x.planName=planA&&planB&&(planA===planB||planA.includes(planB)||planB.includes(planA))?(String(a.planName).length<=String(b.planName).length?a.planName:b.planName):null;
 x.city=norm(a.city)&&norm(a.city)===norm(b.city)?a.city:null;x.state=norm(a.state)&&norm(a.state)===norm(b.state)?a.state:null;
 x.bundleDetected=Boolean(a.bundleDetected||b.bundleDetected);x.internetPriceIsolated=Boolean(a.internetPriceIsolated&&b.internetPriceIsolated);
 const key=e=>`${norm(e?.name)}|${Number(e?.price??-1).toFixed(2)}`;const bkeys=new Set((b.extras||[]).map(key));x.extras=(a.extras||[]).filter(e=>bkeys.has(key(e)));
 x.confidence={...(a.confidence||{})};for(const k of ['provider','internetMonthlyPrice','invoiceTotal','speedMbps','cep']){const av=Number(a.confidence?.[k]||0),bv=Number(b.confidence?.[k]||0);x.confidence[k]=Math.min(av,bv,.95)}if(!x.provider)x.confidence.provider=0;if(!x.speedMbps)x.confidence.speedMbps=0;if(!x.internetMonthlyPrice)x.confidence.internetMonthlyPrice=0;if(!x.invoiceTotal)x.confidence.invoiceTotal=0;if(!x.cep&&!typedCep)x.confidence.cep=0;if(typedCep)x.confidence.cep=1;x.confidence.overall=Math.min(Number(a.confidence?.overall||0),Number(b.confidence?.overall||0),.95);
 x.warnings=[...new Set([...(a.warnings||[]),...(b.warnings||[]),...issues.map(()=> 'DOUBLE_READ_DISAGREEMENT')])].slice(0,12);
 return {extraction:x,issues};
}
function renderExtract(x,p,issues){const cost=p?.billingBaseline?.baselineMonthlyCost??x.internetMonthlyPrice??x.invoiceTotal;txt('#xProvider',x.provider||'Não identificado');txt('#xService','Internet');txt('#xPlan',x.speedMbps?`${x.speedMbps} Mbps`:'Não identificado');txt('#xBill',money(cost));txt('#xAdds',`${Array.isArray(x.extras)?x.extras.length:0} confirmado(s)`);const v=$('.extracted .verified');if(v)v.textContent=issues?.length?'⚠ leitura com divergência':'✓ dupla leitura validada'}
function block(items,title='Análise inconclusiva'){const b=document.createElement('div');b.className='p-real p-block';b.innerHTML=`<h4>${esc(title)}</h4><p>O Poupai preferiu parar a comparação a completar dados que não ficaram comprovados.</p><ul>${(items||[]).map(x=>`<li>${esc(x.message||x.code||x)}</li>`).join('')}</ul>`;$('.extracted')?.insertAdjacentElement('afterend',b)}
function result(p){const a=p.analysis;if(!a){block([{message:p.message||'Sem dados suficientes para comparar.'}]);return}const comps=a.comparisons||[],best=comps[0]||null;const final=p.finalDecision!=='ANALISE_INCONCLUSIVA',decision=final?p.finalDecision:(p.provisionalDecision||'ANALISE_INCONCLUSIVA');const s12=best?Math.max(0,Number(best.estimatedSavings12Months||0)):0,s24=best?Math.max(0,Number(best.estimatedSavings24Months||0)):0;const box=document.createElement('div');box.className='p-real';box.innerHTML=`<div class="p-status"><div><h4>${final?'Recomendação final':'Análise preliminar'}</h4><p>${esc(p.message||'Resultado calculado com esta fatura.')}</p></div><span class="p-badge ${final?'good':'warn'}">${esc(decision.replaceAll('_',' '))}</span></div><div class="p-kpis"><div class="p-kpi"><small>Custo atual</small><b>${money(p.billingBaseline?.baselineMonthlyCost)}</b></div><div class="p-kpi"><small>Economia 12m</small><b>${money(s12)}</b></div><div class="p-kpi"><small>Economia 24m</small><b>${money(s24)}</b></div></div>${p.requiresAddressConfirmation?'<div class="p-note warn">As ofertas abaixo são candidatas. A cobertura exata no imóvel ainda precisa ser confirmada.</div>':''}${comps.length?`<div class="p-offers">${comps.slice(0,6).map((c,i)=>offer(c,i)).join('')}</div>`:'<div class="p-note">Nenhuma oferta comparável passou pelas validações.</div>'}`;$('.extracted')?.insertAdjacentElement('afterend',box)}
function offer(c,i){const o=c.offer||{},confirmed=c.availability==='confirmed'||o.availabilityConfirmed===true;return `<article class="p-offer"><small>#${i+1} • ${confirmed?'cobertura confirmada':'confirmar cobertura'}</small><h5>${esc(o.provider||'Operadora')} • ${esc(o.planName||'Plano')}</h5><div>${o.speedMbps||'—'} Mbps${o.technology?' • '+esc(o.technology):''}</div><div class="price">${money(o.priceMonthly)} <small>/mês</small></div><small>Custo médio 12m: ${money(c.effectiveMonthlyCost12)}</small>${o.sourceUrl?`<a href="${esc(o.sourceUrl)}" target="_blank" rel="noopener">Ver fonte oficial ↗</a>`:''}</article>`}
async function run(){
 if(!selectedFile)selectedFile=$('#fileInput')?.files?.[0]||null;if(!selectedFile){alert('Escolha uma fatura.');return}if(selectedFile.size>3*1024*1024){alert('Arquivo acima de 3 MB.');return}const mt=mime(selectedFile);if(!mt){alert('Formato não suportado.');return}clearReal();
 try{
  const data=await b64(selectedFile);phase(10,'Lendo sua fatura…','Primeira leitura independente do documento.','#c1');
  const r1=await post('/api/read-bill',{filename:selectedFile.name,mimeType:mt,base64:data},60000);
  phase(25,'Conferindo a leitura…','Executando uma segunda leitura independente.','#c2');
  const r2=await post('/api/read-bill',{filename:selectedFile.name,mimeType:mt,base64:data},60000);
  const typed=cleanCep($('#cep')?.value);const merged=conservativeMerge(r1.extraction||{},r2.extraction||{},typed);const x=merged.extraction;if(x.cep&&$('#cep'))$('#cep').value=x.cep;
  phase(38,'Validando os dados…','Só os campos em que as duas leituras concordaram seguem para o motor.','#c2');
  const pre=await post('/api/run-pipeline',{extraction:x,marketResult:{},engineConfig:{}},15000);renderExtract(x,pre,merged.issues);
  const criticalIssues=[];if(!x.provider)criticalIssues.push({message:'Operadora não confirmada nas duas leituras.'});if(!(Number(x.speedMbps)>0))criticalIssues.push({message:'Velocidade não confirmada nas duas leituras.'});if(!(Number(x.internetMonthlyPrice)>0))criticalIssues.push({message:'Preço mensal da internet não confirmado nas duas leituras.'});
  if(criticalIssues.length){done();block([...criticalIssues,...merged.issues]);return}
  if(pre.decisionReason!=='INSUFFICIENT_MARKET_DATA'&&pre.blockingReasons?.length){done();block(pre.blockingReasons);return}
  const cp=cleanCep(x.cep||$('#cep')?.value);if(!cp){done();block([{message:'CEP não confirmado. Digite o CEP da instalação e analise novamente.'}]);return}
  phase(60,'Pesquisando ofertas…','Consultando somente páginas oficiais disponíveis.','#c3');const m=await post('/api/find-offers',{cep:cp,city:x.city||null,state:x.state||null},25000);let market=m.result||{};
  const num=String($('#houseNumber')?.value||'').trim();if(num&&market.offers?.length){phase(78,'Validando cobertura…','Tentando confirmar o endereço.','#c4');const providers=[...new Set(market.offers.map(o=>o.provider).filter(p=>/^(Claro|TIM|Vivo)$/i.test(String(p))))];if(providers.length){try{const ck=await post('/api/check-availability',{cep:cp,number:num,providers,marketResult:market},45000);if(ck.updatedMarket)market=ck.updatedMarket}catch(e){console.warn(e)}}}else $('#c4')?.classList.add('done');
  phase(92,'Calculando o resultado…','Comparando sua conta validada com as alternativas encontradas.','#c5');const p=await post('/api/run-pipeline',{extraction:x,marketResult:market,engineConfig:{}},15000);done();if(p.blockingReasons?.length&&p.decisionReason!=='ADDRESS_AVAILABILITY_NOT_CONFIRMED'){block(p.blockingReasons);return}result(p);
 }catch(e){console.error(e);done();block([{message:e?.name==='AbortError'?'A análise demorou mais que o esperado.':String(e?.message||e)}],'Falha na análise')}
}
function install(){const demo=$('.demo-bar');if(demo)demo.innerHTML='<b>Análise real V2.9</b> • duas leituras da fatura • resultados demonstrativos desativados';$('.demo-scenarios')?.setAttribute('style','display:none!important');if($('#cep')){$('#cep').value='';$('#cep').placeholder='00000-000'}if($('.cep-box')&&!$('#houseNumber')){const w=document.createElement('div');w.className='cep-extra-field';w.innerHTML='<label>Número do imóvel (opcional)</label><input id="houseNumber" maxlength="24" placeholder="Ex.: 120">';$('.cep-box').appendChild(w)}const n=document.createElement('div');n.className='p-note';n.textContent='A fatura é lida duas vezes. Se preço, velocidade ou operadora divergirem, o Poupai interrompe a comparação em vez de adivinhar.';$('.cep-box')?.appendChild(n);$('#fileInput')?.addEventListener('change',e=>selectedFile=e.target.files?.[0]||null);$('#drop')?.addEventListener('drop',e=>selectedFile=e.dataTransfer?.files?.[0]||null);cleanBtn('#pickBtn',()=>$('#fileInput')?.click());cleanBtn('#analyzeBtn',run)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();
})();